export {
  Fragment,
  useEffect,
  useRef,
  useState,
  useCallback,
  useMemo,
  JSX,
  createContext,
  useContext,
} from 'react';
export type {
  ReactNode,
  HTMLAttributes,
  CSSProperties,
  MouseEvent,
  PointerEvent,
  TouchEvent,
  ComponentType,
} from 'react';

export const dblClickProp = 'onDoubleClick' as const;
